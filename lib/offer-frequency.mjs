import offerFrequency from './offer-frequency.cjs';

export const {
  OFFER_FREQUENCY_OPTIONS,
  formatOfferFrequencyLabel,
  isSaleListing,
  resolveOfferFrequency,
} = offerFrequency;

export default offerFrequency;
